import styled from 'styled-components'

export const UserInvitesList = styled('div')({
    display: 'flex',
    flexDirection: 'column',
})

export const UserInvitesListItem = styled('div')({
    alignItems: 'center',
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'space-between',
})
